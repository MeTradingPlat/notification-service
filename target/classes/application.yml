spring:
  application:
    name: notification-service
  threads:
    virtual:
      enabled: true

  profiles:
    active: dev

server:
  port: 8085

# Actuator for health checks
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
